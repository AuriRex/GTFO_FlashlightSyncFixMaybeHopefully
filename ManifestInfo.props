<Project>
    <PropertyGroup>
        <TSName>FlashlightSyncFixMaybeHopefully</TSName>
        <TSAuthor>AuriRex</TSAuthor>
        <TSVersion>0.0.1</TSVersion> <!-- TSVersion empty = GitInfo used instead (if available; check nuget) -->
        <TSWebsite>https://github.com/AuriRex/GTFO_FlashlightSyncFixMaybeHopefully</TSWebsite>
        <TSDescription>Should hopefully fix some of the flashlight syncing issues, maybe.</TSDescription>
    </PropertyGroup>
    <ItemGroup>
        <!-- Add the Dependency strings here, one for each dependency -->
        <TSDependencyStrings Include="BepInEx-BepInExPack_GTFO-3.2.1" InProject="false" />
        <!-- <TSDependencyStrings Include="Some-Other-Dependency-1.2.3" InProject="false" /> -->
    </ItemGroup>
</Project>